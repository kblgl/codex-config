---
name: ux-bug-check
description: 从用户视角扫描代码，查找体验bug。按7类清单（第一印象、操作反馈、状态完整性、流程、认知负担、情绪、用户控制感）逐项扫描，输出按严重度分级的问题清单。供 ux-reviewer agent 调用，也可独立调用。触发词：从用户角度看一下、用户用起来怎么样、有没有体验bug、体验有没有问题。
---

## 核心视角

不代入具体画像，不还原叙事。视角是「一个普通用户用这份产物，会不会卡住、困惑、不爽」。问题来源是普适的人类认知和情绪反应，不是某个具体人物的内心独白。

边界认知：清单擅长抓结构性问题（缺状态、缺反馈、缺出口），抓不住情境性问题（要代入用户真实使用场景才能发现的，如同名对象无法区分）。用户的真实使用反馈是清单替代不了的输入，收到这类反馈优先转成新检查项沉淀。

## 触发

- ux-reviewer agent 调用本skill
- 用户消息含「体验上有没有问题/用户用起来怎么样/有没有体验bug/从用户角度看」

## 不做什么

- 不查代码正确性、AC、安全（那是 code-reviewer）
- 不查工程规范（a11y、性能、触控尺寸、响应式）
- 不算商业账（拉新、留存、变现）
- 不重新设计（不给具体UI规格、不给字号色值，只给改进方向）
- 不要求画像、不要场景代入

## 七类清单

按以下顺序逐类扫，每类Read对应references。

| 类 | 视角 | references |
|----|------|------------|
| 1. 第一印象 | 3秒内能不能看懂 | `references/first-impression.md` |
| 2. 操作反馈 | 按下去有没有反应 | `references/operation-feedback.md` |
| 3. 状态完整性 | 空/加载/错误/边界态全吗 | `references/state-completeness.md` |
| 4. 流程 | 走得顺不顺 | `references/flow-design.md` |
| 5. 认知负担 | 要不要琢磨 | `references/cognitive-load.md` |
| 6. 情绪 | 让人不爽吗 | `references/emotion-design.md` |
| 7. 用户控制感 | 被推着走吗 | `references/user-control.md` |

第6类里的文案专业度问题（解释腔、聊天腔、黑话泄漏）下钻用 `~/.codex/skills/ui-copy-check/SKILL.md`，它比本清单深一层且直接给重写后的文字。

## 执行步骤

1. 接到改动文件清单，Read 改动文件做静态分析（看HTML/JSX/Vue结构、文案、状态处理、错误处理路径）
2. 有可跑界面时通过 webapp-testing skill（`Read ~/.codex/skills/webapp-testing/SKILL.md`）跑起来观察真实交互
3. 七类逐项扫
4. 每条发现打严重度：
   - **阻断**：用户会卡住、不知所措、放弃任务
   - **困惑**：能继续但需要琢磨、容易猜错
   - **不爽**：能用但感觉不舒服
5. 按 `templates/ux-report.md` 输出

## 输出原则

- 每条问题含位置、所属类、用户视角描述、改进方向、严重度
- **分页产物的定位与排序（硬规则，2026-07-15用户圈定）**：审查对象是课件deck、PPT、多页文档这类带页码的产物时，报告必须**按用户可见的页码排序和定位**（「第17页」），页码以产物自身显示的计数为准（如deck右下角的N/37，即该天内的DOM顺序），同一页多条问题合并在该页名下。元素id、slide id、文件行号是用户在放映器里看不到的东西，只能作辅助信息附在页码后，禁止当主定位。给报告前先把id到页码的映射算出来，别让用户自己去猜d2-s1是第几页
- 用户视角描述用「用户看到这里会想……」的形式，不用第三人称工程语言
- 改进方向只给方向不给规格（具体UI改法是 ui-designer 设计模式的事）

## 静态分析能覆盖到的类

静态分析（Read 代码 + Grep 文案 + 看HTML结构）能覆盖：

- 第3类状态完整性：能看出有没有写空状态/loading/错误处理分支
- 第4类流程：看路由、步骤组件、表单结构
- 第5类认知负担：看按钮样式、链接样式、信息层级类名
- 第6类情绪：看错误信息文案、删除确认逻辑
- 第7类用户控制感：看是否有退出/撤销/关闭逻辑

需要实跑才能确认的：

- 第1类第一印象：要看真实首屏渲染才能判断3秒内能否看懂
- 第2类操作反馈：要看按钮按下态、loading反馈、过渡动画

实跑不到时在报告里标注「未实跑，1/2类基于静态结构推断」。

## 语言规范

输出遵守 `~/.codex/rules/no_ai_style.md`。
