# 派工模板

版本：v1.1（2026-07-08建，07-13：T1拆三段、段间人工过门）

## 弱模型派工设计规则（写任何派工prompt前先看）

1. 一次派一个任务，不打包多任务
2. 全部用绝对路径，禁止「项目里的那个文件」式指代
3. 产出物路径预先指定，不让执行者自己决定放哪
4. 步骤编号，每步有产物，中断可从断点续跑
5. 完成标准优先可机器验证（文件存在、条数相等、字段齐全），主观标准给判例
6. 执行者拿不准的列「待确认项」集中汇报，禁止自作主张选一个
7. 凭证只给存放位置，明文永不进prompt
8. 执行者第一步永远是Read列出的文件，禁止凭训练记忆执行流程

## 模板骨架（所有派工prompt的固定结构）

```
你是<角色>，任务：<一句话>。

## 先读（按序Read，读完再动手）
- <流程文档绝对路径>
- <输入文件绝对路径>

## 已确认的决定（直接采信，禁止重新探索或质疑）
- <人已拍板的结论>

## 执行步骤
1. <步骤> → 产物：<绝对路径>
2. ...

## 禁止
- <红线>

## 完成标准（可验证）
- <标准>

## 汇报格式
产物路径清单 / 完成标准逐条勾选结果 / 待确认项
```

---

## T1 课件生产（大纲已确认后，分三段派，段间人工过门）

live-lesson-deck的六道门里，大纲之后还有草模走查、风格预览两道人工门（口喷转写门在大纲之前，派T1时应已过或已明确跳过）。subagent中途没有对话通道，所以外派必须拆成三段工作包，段间由本体把产物交用户确认后再派下一段，禁止一口气跑完。默认优先留主线边做边过门，外派是需要节省主线上下文时的选择。

**T1a 草模**

```
你是课件制作者，任务：为<项目名>产出课件草模（结构稿）。

## 先读（按序Read，读完再动手）
- /Users/zrf/.claude/skills/live-lesson-deck/SKILL.md（流程权威）
- <大纲文件绝对路径>（人已确认的主线）
- <项目目录绝对路径>（课件内容来源）

## 已确认的决定
- 大纲即取舍结论：不在大纲里的主题不做，大纲里的主题和顺序不改
- 目标学员：<一句话画像>

## 执行步骤
1. 按大纲产出全部页面的灰模结构稿（每页只放标题、「这页回答的问题」一行、素材占位块，这是SKILL的草模口径，不做视觉风格） → 产物：<项目目录>/lesson-draft.html

## 禁止
- 增删页面主题或改顺序（发现大纲问题列进待确认项，不自己改）
- 做视觉风格（那是T1b的事）

## 汇报格式
产物路径 / 页数与大纲对照 / 待确认项
```

**T1b 风格样张**（T1a经用户走查后再派）

```
你是课件制作者，任务：为<项目名>课件出视觉风格样张。

## 先读
- /Users/zrf/.claude/skills/live-lesson-deck/SKILL.md
- <项目目录>/lesson-draft.html（已过走查的草模）

## 已确认的决定
- 草模走查结论：<用户对页序和覆盖度的意见，没有就写「原样通过」>
- 风格方向：<用户点名的风格或已有视觉基线；都没有就写「未指定」>

## 执行步骤
1. 按SKILL的风格预览口径出样张：风格未指定 → 出3版差异化首屏预览供挑选；点名了风格或有视觉基线 → 实作1版代表页 → 产物：<项目目录>/lesson-style-preview.html

## 禁止
- 铺全部页面（那是T1c的事，样张未经用户认可不做后续页）

## 汇报格式
产物路径 / 每版风格决策一句话（字体、配色、版式）
```

**T1c 铺全**（T1b经用户认可后再派）

```
你是课件制作者，任务：按已认可的风格样张铺完<项目名>全部课件页。

## 先读
- /Users/zrf/.claude/skills/live-lesson-deck/SKILL.md
- <项目目录>/lesson-draft.html 和 lesson-style-preview.html
- <口喷转写稿绝对路径>（叙事源，走了口喷环节时必传；上墙字从这里提炼，不另起炉灶创作）
- <项目目录绝对路径>（素材源，截图、代码、演示从这里取真实内容）

## 已确认的决定
- 风格样张结论：<用户认可的风格及修改意见>

## 执行步骤
1. 按样张风格铺完全部页面，草模的素材占位块逐个替换为真实素材（从项目目录取，拿不到的列入待确认项） → 产物：<项目目录>/lesson.html
2. 浏览器自检：翻页正常、console无报错
3. 按 ~/.claude/personal-ai-os/checklist.md 的B1组九条逐条自检

## 禁止
- 偏离已认可的风格样张
- 按项目的制作时间线铺页
- 交付含未声明占位块的成品（B1第8条）

## 完成标准
- lesson.html存在且左右键可翻页
- B1九条全过，或列出未过条目和原因

## 汇报格式
产物路径 / B1逐条勾选结果（带编号）/ 待确认项
```

## T2 直播答疑归档

```
你是答疑归档执行者，任务：完成<YYYYMMDD>期直播答疑归档。

## 先读（按序Read，读完再动手）
- /Users/zrf/.claude/skills/live-qa-archive/SKILL.md（九步流程，全文照做）
- <归档项目目录绝对路径>/AGENTS.md

## 已确认的决定
- 当期日期：<YYYYMMDD>
- 凭证位置：~/.claude/rules/feishu_credentials.local.md（只进环境变量，明文不进任何文件和输出）

## 执行步骤
按SKILL九步执行，产物落 <归档项目目录>/out/<YYYYMMDD>/。
到第7步（精炼要点）停下，把today.json路径交回，等today_refined.json就位再继续第8步。

## 禁止
- 自己编造精炼要点跳过第7步
- 覆盖往期任何产物

## 完成标准
- ~/.claude/personal-ai-os/checklist.md 的B3组五条全过

## 汇报格式
全部产物路径 + wiki URL / B3逐条勾选结果 / 两件人工待补事项
```

## T3 B站文稿初稿（林亦LYi风格）

```
你是文稿写手，任务：按林亦LYi风格为选题<选题>产出B站视频文稿。

## 先读（按序Read，读完再动手）
- /Users/zrf/.claude/skills/linyi-lyi-scriptwriter/SKILL.md（流程权威，含三模式入口、生产流水线、自检8条）

## 已确认的决定
- 模式：<成稿 / 改稿 / 骨架，人已定>
- 核心观点：<人已定，禁改>
- 目标时长：<N>分钟（按skill口径，8到12分钟对应3000到4500字）
- 真实素材：<用户给的实验数据、个人经历；没给就写「无，实验剧情按示意标注」>

## 执行步骤
1. 按SKILL对应模式的流水线走 → 产物：/Users/zrf/workspace/bilibili-scripts/drafts/<YYYYMMDD>-<slug>.md
2. 按SKILL自检8条逐条过并修到过
3. 按 ~/.claude/personal-ai-os/checklist.md 的B2组自检；与林亦风格冲突的条目不强改，列进汇报的冲突标注

## 禁止
- 改核心观点
- 把编造的实验数据、个人轶事当真实交付（没有真实素材必须逐处标注「示意，需替换为实测」）

## 完成标准
- SKILL自检8条全过
- 全文字数在目标时长对应区间内
- 所有虚构素材已逐处标注

## 汇报格式
产物路径 / 自检8条勾选结果 / B2冲突标注 / 待确认项（需用户替换的示意素材清单、拿不准的事实）
```

## T4 微信读书笔记落盘

```
你是笔记归档员，任务：把下面粘贴的微信读书划线整理进知识库。

## 目标目录
/Users/zrf/workspace/personal-note/obsidian-vault/Notes/<YYYY-MM>/

## 规则
- 每本书一个md文件：<书名>-读书笔记.md。文件已存在则在末尾追加新划线，不覆盖已有内容
- frontmatter三字段：created（ISO日期）、tags（含「读书笔记」）、book（书名）
- 每条划线一个bullet；我写的想法用引用块（>）紧跟在对应划线后
- 双链用短路径[[概念名]]，只链vault里已存在的同名笔记，不确定就不加

## 禁止
- 改写划线原文
- 删除文件里已有的任何内容

## 完成标准
- frontmatter三字段齐全
- 落盘划线条数 = 输入条数（汇报里给两个数字对照）

## 汇报格式
产物路径 / 条数对照（输入N条，落盘N条）/ 加了哪些双链

## 输入
<粘贴划线文本>
```

## T5 每周巡库

```
你是知识库管理员，任务：对知识库做每周巡库。

## 目标目录
/Users/zrf/workspace/personal-note/obsidian-vault/

## 执行步骤
1. 找出Notes/下最近7天新增或修改的md文件（按文件mtime）
2. 逐篇检查：frontmatter缺字段的补上；正文里出现的、vault中已有同名笔记的概念加[[双链]]（短路径）
3. 更新Concepts/：新笔记按主题挂到已有Concept索引页；同一主题攒到3篇以上且没有索引页的，新建一页列出这些笔记的链接
4. 巡库报告追加到 obsidian-vault/Concepts/_巡库日志.md（新增几篇、补了几个链、Concepts改动清单）

## 禁止
- 删除或合并任何笔记
- 改写笔记正文（只允许加链接、补frontmatter缺字段）

## 完成标准
- 本周每篇新笔记都出现在至少一个Concept索引页里
- _巡库日志.md有本次记录

## 汇报格式
巡库报告原文
```

## T6 编码

编码派工模板见`~/.claude/rules/sub_agent_dispatch.md`的「调用code-writer的工作包模板」和「调用reviewer的prompt模板」，那份是权威，此处不复制。跨工具执行时同样把模板内容完整贴进prompt，并遵守本文件开头的弱模型设计规则。
